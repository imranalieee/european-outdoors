import Styled from 'styled-components';

const PurchasingWrapper = Styled.div`
.queue-items-wrapper{
  min-width: 768px;
  overflow-x: scroll;
  overflow-y: hidden;
  h3{
    padding-right: 24px;
  }
  button{
    white-space: nowrap;
  }
  span{
    white-space: nowrap;
  }
}
`

export default PurchasingWrapper;
